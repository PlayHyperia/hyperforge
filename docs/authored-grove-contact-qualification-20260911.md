# Authored grove and actual character contact — 2026-09-11

## Scope

The same five authored, harvestable trees are redistributed around the preparation
clearing. This is not a new decorative forest or a finished island-wide woodland.
Eight procedural trees remain unchanged. Resource identities, species/variants,
scales, yields, original area membership, safety/death policy, bounds and grading
remain unchanged. There is still one arena and no retained large island.

The source candidate is saved and pushed as game checkpoint
`84a18a123302e5d8114dff47f597bede8dfd8877` and assets
`e26cc737543f4762fd421bca75ccea27edba9c17`. GitHub confirms author and committer
dreaminglucid for both. Scoped staged secret scans and normal hooks pass; unrelated
work is preserved. No merge or deployment was performed. No default render profile, avatar body,
rest pose, weights, equipment fit or gameplay population change is included.

## Placement and durable identity

| Durable tree ID | Species | Authored X/Z | Preparation distance |
| --- | --- | --- | --- |
| tree_367_311 | General | 366.5, 322.5 | 16.5 tiles |
| tree_373_313 | Oak | 318.5, 312.5 | 31.5 tiles |
| tree_379_311 | Maple | 323.5, 288.5 | 31.5 tiles |
| tree_379_304 | Mahogany | 381.5, 329.5 | 31.5 tiles |
| tree_375_299 | Magic | 318.5, 344.5 | 31.5 tiles |

Distance follows the existing launch validator's Chebyshev envelope from the
hub midpoint (350,320), not actual walking distance. The 32-tile gate is unchanged.
Manifest SHA-256:
`17bcab39cdde58fe16e0d44fca48344044dd81f3f92f82bb7171bf201c3375c9`.

Explicit authored tree `instanceId` preserves old coordinate-derived custody keys
when a layout changes between world lifetimes. DataManager validates before
publishing either CDN or filesystem manifests. Invalid/non-tree/procedural
overrides, duplicate IDs and duplicate snapped tree anchors fail. Live conflicting
registrations remain rejected; this is not a runtime teleport mechanism.

The grove test uses actual World, ResourceSystem, TerrainSystem, collision matrix,
pathfinder and authored water/flat grades. Transformed LOD0 GLB accessor bounds
include source node transforms, unlike some legacy model-bounds metadata.
All five crowns clear tested path blends, station bounds, NPC margins and the
pond envelope, with four grounded cardinal approaches. These declared bounds
are conservative geometry, not decoded alpha silhouettes, wind or rendered pixels.
The general tree's nearest NPC envelope margin is only 0.24m and requires visual
review. The magic canopy overlaps part of the bandit encounter region.

CPU routes respect the real search cap and continue partial segments. They do not
exercise the live movement scheduler, complete station collision or moving NPC
occupancy. Actual walking/gathering/depletion/regrowth/reconnect remain open.

The final census has 13 unique IDs and snapped anchors, no authored/procedural or
other-resource anchor collisions. Procedural generator/configuration are unchanged
from probe35. Nearest authored/procedural centres are 14.32m apart; this is not a
claim that every canopy is disjoint or that all 13 trees render simultaneously.

## Rejected intermediate placements

The first magic-tree candidate failed the actual transformed crown/pond clearance;
it was moved without shrinking the tree or waiving the test. Probe36 then correctly
stopped at launch preflight: four tree anchors exceeded the existing route envelope
by 0.5–2.5 tiles. No game/browser image was captured. Its failure archive remains
intact; final positions were moved inward and native launch validation now passes.

## Root verification

- Shared resource/placement regression: 84 cases across eight files; final grove
  geometry/envelope case separately rerun after the inward adjustment.
- Contact harness: 48 cases. Full spatial successor: 80 cases; only the retired
  historical eight-floor assertion is excluded, with its complete three-floor
  replacement retained and passing. Historical executed harnesses are immutable.
- Real PostgreSQL: three cases across two files pass. The new test depletes all
  five old identities, closes actual worlds/pools, loads final authored positions
  and water/flat grades in new owners, and compares original absolute deadlines
  and durable records exactly. This is not an OS-server or browser restart test.
- Shared/client/server typechecks, scoped lint and normal client/fresh-server
  builds pass. Competitive build prefix `5fadbdfcbd93`. Owned PostgreSQL test
  databases/containers are removed; unrelated user services are preserved.

Suite totals overlap; they are not counts of independent gameplay scenarios.

## Probe37: actual daylight contact, still visually unapproved

Actual headful Chrome/Metal WebGPU, 1280x720, DPR1, AA4 and the existing explicit
4096-shadow candidate. Run 16:39:10.040–16:41:14.532 UTC. Both actors use real
replicated owners and equipped skinned boots. Six daylight views preserve ordinary
UI and separately capture the exact renderer canvas. World-rendered inventory
grids remain in the canvas: these diagnostics are not wholly UI-free.

The contact collector reads native CPU morph/skinned vertices under current
matrixWorld and intersects the retained LobbyFloor triangles. It never updates
pose, skeletons, matrices, materials, camera or actor transforms. All 24 capture
boundaries have finite coverage and unchanged owners. Before/after samples may
have different natural animation poses; this is not exact screenshot/GPU readback.

| Actor | Left sole minimum across samples | Right sole minimum across samples |
| --- | --- | --- |
| First | +79.03 to +79.97mm | +75.60 to +76.62mm |
| Second | -10.97 to -10.03mm | -14.40 to -13.38mm |

Both stand over the same visual top Y=28.8393015291. Their root Ys differ by exactly
9cm (28.9193015231 versus 28.8293015231). These data corroborate visible floating
and clipping. They are defects, not passing fit/animation results. Correct the
authoritative support contract before using shadow bias/AO to disguise contact.

Probe37 focused study passes; **the whole run fails** the inherited 19 cow/dagger
content errors and cow404. It records zero GPU/page errors and clean owned
cleanup, 36 HTTP200 action receipts, 368 source pins and 298 archives. This is
instrumented correctness evidence, not smoothness, delivered stream or launch
approval. Pale skin, flat dark armor, coarse detached shadows and empty tile
campus remain below the desired presentation quality.

- Report SHA: `3897dbb2ae80db6f339865ba1b9770951522edaf205659a503e8e8fa6fbbba16`
- Contact study SHA: `b096ce01f76b698c8c55e01a46f299cc0fd1e9b1b5e354779439b81be00a3662`
- Built client SHA: `f4cc805a3077b5ceb08bd8f5289b69a923411a805314e4e5349941e02b1b9a17`

Local evidence lives under
`asset-studio/game-test-integration/compact-world-probe37/`. Capture archives are
not made portable merely by committing this document. Full relocated-grove
gameplay/performance/streaming acceptance remain open.

## Probe38: relocated grove in the full spatial scene

Run 16:41:49.920–16:44:22.985 UTC, same resolution/profile/population but the
lightweight diagnostic mode. Full spatial study passes all 11 views; 28 PNGs,
37 HTTP200 actions, 372 source pins and 303 archives. Zero recorded GPU/page
errors and clean owned cleanup. The whole run still fails inherited cow/dagger
content errors/cow404. No error gate is waived.

Root reviewed the actual hub, tree and whole-island images. The five crowns now
frame the clearing instead of forming a crowded specimen row around the altars.
The general-tree trunk and shadow are readable, but foliage overhang still needs
live agent/camera review. The scene is **not visually approved**: the island is
still a broad green mound, most of the southern half is empty, the ground reads
as uniform turf and disconnected rectangular campus pads remain conspicuous.
Tiny material adjustments alone will not deliver the intended quality.

The next landscape candidates are a stronger asymmetric coastal ridge/inlet,
coherent coastal/meadow/forest-floor substrate using the existing packed maps,
and unified weathered campus paving/working aprons. Preserve real terrain-worker
parity, functional grades, navigation, resource ownership and measured budgets.
Current stream grass spacing is approximately 14m before rejection; it cannot
produce lush meadow coverage at that density. Any density change needs an
explicit new population/cost comparison, not a silent performance shortcut.

Post-equip ten-second RAF sample: p95=25ms, maximum=34.2ms; post-study p95=9ms,
maximum=25.1ms. These are display scheduling intervals under different moving
cameras, **not rendered FPS, GPU times or a controlled before/after benchmark**.
They do not replace the prior stall evidence or qualify sustained smoothness.

- Report SHA: `7d0f0718213f917a6006e2fc2472df737c39348767f0f6de11134e1c02644e3a`
- Spatial study SHA: `4598f14b6eb8df540424cb59d4d5ab5b6e18292d07f212b8cdb14ed63d453f72`

Local evidence: `asset-studio/game-test-integration/compact-world-probe38/`.
Grounding follow-up must reconcile the real 0.10m spawn/0.01m movement clearance,
the 0.40m navigational/0.42m solid platform top, and client interpolation. Keep
terrain grading and avatar geometry separate from player support; do not globally
shift resources, grass or the approved body to fix player contact.
